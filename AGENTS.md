# BATON GO 작업 지침

## 작업 기준

- 사용자 요청과 대화에서 정한 범위에 맞춰 작업을 끝낸다. 세부 구현 방식은 직접 결정하고,
  결과를 결정할 정보가 없을 때만 질문한다. 지시 때문에 중단하면 해당 파일과 근거를 설명한다.
- 응답·문서·주석·PR은 간결한 한국어로 작성한다. API 식별자·명령·표준 용어는 유지하고,
  변경을 마치면 `Fix`·`Refactor`·`Docs` 등 종류별로 한글 커밋한다.
- 시작할 때 `README.md`와 `HANDOFF.md`를 확인한다. 같은 세션에서 읽은 파일은 변경 여부와 관련 부분만
  확인한다. 출력이 잘리면 필요한 줄만 다시 읽고 전체 검색·출력을 반복하지 않는다.
- 제품 동작은 [제품 명세](docs/PRD/0001_product-baseline/spec.md), HTTP 동작은
  [API 계약](docs/PRD/0002_api-contract/spec.md)을 따른다. 변경에 필요한 문서만 읽고,
  제품 요구는 PRD, 장기 설계는 ADR, 운영 절차는 RUNBOOK, 현재 인계 상태는 HANDOFF에 기록한다.

## 서비스와 모듈

- GO는 링크 코드·활성 시간·만료·폐기·대상 라우팅을 관리한다. BATON의 작업 공간·역할·회차
  권한과 ROUND의 방 입장 권한은 각 서비스가 판단한다.
- BATON의 저장 트랜잭션이나 작업 공간 조회 안에서 원격 링크를 생성하지 않는다.
  생성·폐기 요청과 멱등성 키를 원본 상태와 함께 저장하고, 커밋 후 같은 키로 재시도한다.
- 의존 방향은 `bootstrap → adapters → application → domain`과
  `guard-tool → adapter-out-external/application → domain`이다.
  `domain`은 상태·시간 규칙, `application`은 사용 사례와 포트, `adapter-in-web`은 HTTP,
  `adapter-out-persistence`는 JPA·SQL, `adapter-out-external`은 코드 발급·대상 URL 조립,
  `guard-tool`은 기존 DB의 HMAC 키 최초 등록, `bootstrap`은 설정·Flyway를 담당한다.
  컨트롤러에 업무 규칙을 두거나 application에서 adapter를 참조하지 않는다.

## 구현 규칙

- 기존 코드와 JDK·Spring·사용 중인 라이브러리를 먼저 사용한다. 계약 의미가 달라 직접 구현하면
  차이를 테스트나 관련 문서에 남긴다. 실제 중복이나 책임 분리가 필요할 때만 추상화를 추가한다.
- Java 21을 사용하고 업무 시각은 주입된 `Clock`으로 조회한다. 절대 시각은 `Instant`와 UTC DB 값으로
  저장한다. 달력 기준 계산이 필요할 때만 명시적인 `ZoneId` 정책을 둔다.
- 신규 생성은 정규 UUID `Idempotency-Key`를 요구한다. 32자 이상 별도 비밀값과 HMAC-SHA-256으로
  공개 코드 128비트를 파생하며, DB에는 공개 코드·멱등성 키의 SHA-256 해시만 저장한다.
- 대상은 승인된 시스템의 `/`로 시작하는 경로만 허용한다. 스킴·호스트·쿼리·프래그먼트·`//`는 거부한다.
- 공개 `GET·HEAD`는 상태나 사용 횟수를 바꾸지 않는다. 일회성 교환은 인증 후 별도 `POST`로 처리한다.
- 오류는 `UPPER_SNAKE_CASE` 코드, 사용자용 메시지, 선택적 `requestId`를 사용한다.
- 적용된 Flyway 파일을 수정하지 않고 다음 버전을 추가한다. 비밀값과 환경별 주소는 환경 변수로 주입한다.
- 접근 키·참여 허가·Bearer 토큰·세션 ID를 링크 대상이나 로그에 넣지 않는다.
  멱등성 키·원문 링크 코드·전체 단축 URL·대상 경로·HMAC 비밀값도 로그에 남기지 않는다.

## 검증

- 입력 검증은 해당 규칙을 담당하는 계층에 둔다. HTTP·DB 동시성·Spring 빈 구성처럼 따로 실패할 수 있을 때만
  다른 계층에서도 검증한다. 검증을 정리할 때 오류 코드와 실패 시점도 보존한다.
- 작업을 시작할 때 기준 리비전과 기존 변경 파일을 기록한다. 파일을 작성하거나 수정한 직후 해당 파일을
  다시 읽고 `git diff --check -- <파일>`과 파일 형식에 맞는 가까운 검사를 실행한다. 새 파일은 `git diff`에
  나오지 않을 수 있으므로 내용 전체를 직접 확인한다.
- 한 동작의 변경을 마치면 해당 모듈의 컴파일·대상 테스트를 실행한다. Java 패키지나 모듈 의존을 바꿨다면
  `./gradlew --no-daemon :bootstrap:test --tests '*ArchitectureRulesTest'`로 계층 규칙도 확인한다.
- 커밋 전에는 해당 변경 묶음의 전체 diff를 읽는다. 작업 종료 직전에는 `git status --short`,
  `git diff --check`, 시작 리비전부터 현재 `HEAD`까지의 diff와 남은 미커밋 diff를 함께 확인한다.
  깨끗한 작업 트리만 보고 전체 변경 검토를 생략하지 않는다.
- 변경한 동작에 필요한 기존 테스트를 먼저 실행한다. 외부 동작·상태 규칙을 검증하고,
  같은 입력 조합의 중복 테스트, 라이브러리 자체 테스트, 테스트 전용 운영 API는 만들지 않는다.
- 필요한 검증이 통과하면 끝낸다. 새 변경·실패·미해결 문제가 있을 때만 검증을 확대하거나 반복한다.
  문서·주석만 바꾸면 내용과 참조를 확인한다. 테스트의 `@DisplayName`은 한국어 문장으로 작성한다.
- 변경별 명령과 결과 재사용은 [개발 검증 절차](docs/RUNBOOK/development-verification.md)를 따른다.
  일반 테스트 성공을 MySQL·Redis 통합 검증 성공으로 보고하지 않는다.
